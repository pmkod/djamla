export const reactPaginationExampleCode = `import { Pagination } from "../core/pagination";

export const PaginationExample = () => {
  return <Pagination defaultPage={1} count={500} pageSize={10} />;
};
`